# Task 1 — DONE (auto-login working on local docker `idre` DB)

**Date:** 2026-05-17
**Supersedes:** `2026-05-17-task1-snapshot-pivot.md` (snapshot pivot — deferred)

## What works now

- `idre/.env` `DATABASE_URL=mysql://root:idrelocal@localhost:3306/idre`
- IDRE auto-login at `http://127.0.0.1:3000/api/dev/auto-login` → returns 307 with `set-cookie: better-auth.session_token=...` → redirects to /dashboard
- Auth substrate: 47 seeded users in `idre` DB, Ryan@orchid123 (hardcoded seed hash in `seeds/utils/auth-helpers.ts`)
- Data substrate for Phase B validation: 47 users / 36 cases / 43 payments (dev seed data)

## What actually broke for ~4 hours

The original `.env.localmysql.backup` pointed at `mysql://root:idrelocal@localhost:3306/orchid-idre` — but **`orchid-idre` DB on local docker is EMPTY**. The seeded data lives in a sibling DB called `idre`. The backup represented a non-working state.

Symptoms we wasted time on:
- "Auto-login fails on staging" — chased SSL params, password hunting (46 scrypt candidates), considered modifying IDRE on main
- "Snapshot staging → local docker" — 3 failed mysqldump attempts (90 min) due to Windows binary-redirect + Git Bash path mangling + RDS-over-WAN bandwidth
- All while the actual fix was a one-line `.env` edit to point at the existing seeded DB

## Spec §4 status

Original spec required IDRE and bot to read the **same DB** so UI numbers can validate bot numbers. With this resolution:
- IDRE reads local docker `idre` (47 users / 36 cases dev data)
- Bot still reads staging RDS (where Anand's actual derived-query tests target)

These are DIFFERENT databases — spec §4 isn't satisfied for production-scale validation. Acceptable for THIS iteration because:
1. We're testing whether bot SQL logic matches IDRE display logic (which can be validated at any data scale)
2. Production-data parity is a follow-up snapshot effort (deferred — see below)

## Deferred: production-data snapshot

When/if needed for catching scale-only SQL bugs:
- **Path:** AWS RDS snapshot export to S3 (parquet) → conversion to MySQL dump → local docker import. Bypasses the ~4-7 MB/min WAN bottleneck that killed the mysqldump approach.
- **Trigger:** if Phase B baseline reveals bot SQL that returns wrong shapes on small data but might be hiding scale bugs.
- **Owner:** TBD when we get there.

## Key files (final state)

- `idre/.env` — `DATABASE_URL=...idre` (the working DB, NOT `orchid-idre`)
- `idre/.env.localmysql.backup` — kept as-is for historical reference (points at empty `orchid-idre`); do NOT use as a revert target without first re-pointing at `idre`
- `idre/app/api/dev/auto-login/route.ts` — original 28-line version, hardcoded Ryan@orchid123
- `.snapshots/` — gitignored; contains pre-built `reset_ryan_password.py` (unused — kept for the deferred snapshot path)

## Verification done

- `curl -I http://127.0.0.1:3000/api/dev/auto-login` → 307 + `set-cookie: better-auth.session_token=...`
- Sample query against local docker `idre` confirmed Ryan exists with role=master-admin
- IDRE startup log shows `deploymentEnvironment: 'LOCAL'` and clean compile

## Moving on

- Task 2 (Playwright install) starts next
- Tasks 3-12 proceed against `idre` local docker data
- Snapshot pivot doc preserved for future reference but the snapshot itself is deferred
